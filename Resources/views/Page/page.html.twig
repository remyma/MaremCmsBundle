{% extends 'LeBonCadreShopBundle::layout.html.twig' %}

{% block content %}
    <div class="page-header">
        <h1 class="page-title">{{ content.title|raw }}</h1>
    </div>

    {{ content.body|raw }}
{% endblock %}

